# Installation instructions

In the Julia REPL, simply type `]` and
```julia
(@v1.8) pkg> add https://github.com/grizzuti/AbstractLinearOperators.git, add https://github.com/grizzuti/UtilitiesForMRI.git
```
The package `AbstractLinearOperators` has to be explicitly installed since it is currently unregistered.